import 'package:flutter/material.dart';

class TraceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Trace')),
      body: Center(child: Text('Trace'),),
    );
  }
}